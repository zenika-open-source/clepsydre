{
  "name": "Presentation Timer",
  "version": "0.0.1",
  "description": "Fullscreen timer with colors filling the screen for presentations",
  "private": true,
  "keywords": [
    "presentation",
    "timer"
  ],
  "license": "MIT",
  "author": "David Da Silva",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "echo \"Just open the file in your browser\" && exit 0",
    "build": "echo \"No need for that\" && exit 0"
  }
}
